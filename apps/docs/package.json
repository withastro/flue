{
	"name": "apps-docs",
	"type": "module",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"dev": "astro dev",
		"build": "rm -rf ./dist && astro build && mv ./dist/docs/_redirects ./dist/_redirects && mv ./dist/docs/_headers ./dist/_headers && pagefind --site ./dist/docs",
		"preview": "astro preview",
		"deploy": "pnpm run build && pnpm exec wrangler deploy",
		"deploy:nightly": "pnpm run build && pnpm exec wrangler deploy --env nightly",
		"astro": "astro",
		"check:types": "tsc --noEmit"
	},
	"dependencies": {
		"@astrojs/mdx": "^7.0.3",
		"@paper-design/shaders": "0.0.77",
		"@tailwindcss/vite": "^4.2.0",
		"astro": "^7.0.9",
		"tailwindcss": "^4.2.0"
	},
	"devDependencies": {
		"pagefind": "^1.5.2",
		"typescript": "^7.0.2",
		"vite": "^8.0.14",
		"wrangler": "^4.113.0"
	}
}
