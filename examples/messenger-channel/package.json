{
	"name": "messenger-channel-example",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "vite build",
		"check:types": "tsc --noEmit",
		"test": "pnpm run test:node && pnpm run test:workerd",
		"test:node": "vitest run",
		"test:workerd": "vitest run --config vitest.workerd.config.ts"
	},
	"dependencies": {
		"@flue/messenger": "workspace:*",
		"@flue/runtime": "workspace:*",
		"hono": "4.12.32",
		"valibot": "^1.0.0"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "0.19.1",
		"@flue/cli": "workspace:*",
		"@flue/vite": "workspace:*",
		"typescript": "^7.0.2",
		"vite": "^8.0.14",
		"vitest": "^4.1.6"
	}
}
