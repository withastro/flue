# @flue/sdk

This package is a migration placeholder. The old `@flue/sdk` runtime has moved
to `@flue/runtime`, and build/dev tooling has moved to `@flue/cli`.

See the changelog for migration details:
https://github.com/withastro/flue/blob/main/CHANGELOG.md#unreleased
